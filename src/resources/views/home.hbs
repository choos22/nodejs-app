<div class="mt-4">
    <div class="row">


        {{#each products}}

        <div class="col-sm-6 col-lg-3">
            <div class="card card-products-item" style="width: 18rem;">
                <a href="/products/{{this.slug}}">
                    <img src="{{this.img}}" class="card-img-top" alt="{{this.name}}">
                </a>
                <div class="card-body">
                    <a href="/products/{{this.slug}}">
                        <h5 class="card-title">{{this.name}}</h5>
                    </a>
                    <p class="card-text">{{this.desciption}}</p>
                    <a href="/products/{{this.slug}}" class="btn btn-primary">Mua Hàng</a>
                </div>
            </div>
        </div>

        {{/each}}


    </div>
</div>